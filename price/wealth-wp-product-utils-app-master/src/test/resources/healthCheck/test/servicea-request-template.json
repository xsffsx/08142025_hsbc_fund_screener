{
  "applicationCommon": {
    "countryCode": "${countryCode}",
    "groupMember": "${groupMember}"
  },
  "criteria": {
    "productIdList": [{
      "countryCode": "${countryCode}",
      "groupMember": "${groupMember}"
    }
    ]
  }
}