# DB profile
spring:
  datasource:
    url: jdbc:hsqldb:mem:testdb
    username:
    password:
    driver-class-name: org.hsqldb.jdbcDriver
  batch:
    job:
      enabled: false
  task:
    execution:
      pool:
        core-size: 1
  data:
    mongodb:
      host: localhost
      port: 27017
      database: admin
      username: 
      password: 
      enable-replica: false
