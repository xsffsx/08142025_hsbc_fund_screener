spring:
  datasource:
    url: jdbc:hsqldb:mem:testdb
    username:
    password:
    driver-class-name: org.hsqldb.jdbcDriver
  batch:
    job:
      enabled: false
  data:
    mongodb:
      host: localhost
      port: 27017
      database: admin
      username:
      password:

batch:
  chunk-size: 1
  soap: https://test
